#!/usr/bin/env rake
Dir['lib/tasks/**/*.rake'].each { |task| load task }

# GEM_SERVER = 'http://...'
# require 'geminabox/rake'
# Geminabox::Rake.install dir: './',
#                         name: 'hsm',
#                         host: GEM_SERVER
